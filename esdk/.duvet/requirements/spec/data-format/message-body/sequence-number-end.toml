target = "spec/data-format/message-body.md#sequence-number-end"

# Sequence Number End
#
# An indicator for the Final Frame.  
# The value MUST be encoded as the 4 bytes `FF FF FF FF` in hexadecimal notation.

[[spec]]
level = "MUST"
quote = '''
The value MUST be encoded as the 4 bytes `FF FF FF FF` in hexadecimal notation.
'''

